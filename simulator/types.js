var _instruction = exports.instruction = function (type, opts, onError) {

    this.type = type;
    this.opts = opts;


    /*
    switch (type) {

        case 'newClient' :{
            var valid = typeof(opts.x)

        }
        break;

        default :{
            onError('Unrecognised instruction type: ' + type);
        }
    }
    */
}